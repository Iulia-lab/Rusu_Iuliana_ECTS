@page
@model Rusu_Iuliana_ECTS.Pages.Biscuits.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Prajituri</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Biscuit[0].Denumire)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Biscuit[0].Ingrediente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Biscuit[0].Pret)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Biscuit[0].DataExpirarii)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Biscuit[0].Publisher.PublisherName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Biscuit) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Denumire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ingrediente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pret)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataExpirarii)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Publisher.PublisherName)
                </td>

            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
